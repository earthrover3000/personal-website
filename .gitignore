# Built output - intentionally tracked for GitHub Pages deployment
# pages/public/life-clock/ is tracked
# pages/protected/ is tracked

# Source code and scripts (local only, not deployed)
src/
scripts/

# Private folder (sensitive content)
pages/private/

# Planning docs (MEGA-backed, not needed in git)
docs/
